---
topic: "logscan"
status: "ready"
human_approval: "approved"
human_approval_note: "2026-08-22 사용자가 implement 오케스트레이션 병렬화 측정을 위한 고정 워크로드로 이 PRD를 승인했다."
review_profile: "standard"
review_rationale: "로컬 파일만 읽는 오프라인 CLI로 인증, 결제, 네트워크 호출, 서버 데이터, 되돌릴 수 없는 부수효과가 없다. 쓰기 대상은 표준 출력뿐이다."
source_intake: "current conversation"
created_at: "2026-08-22"
updated_at: "2026-09-08"
---

# PRD: logscan

## Goal

저장소 루트의 `logscan/`에 설치와 빌드 없이 실행되는 오프라인 로그 포렌식 CLI를 만든다.
고정 로그 파일을 읽어 독립적인 탐지기 5종을 실행하고 심각도 순 리포트와 JSON을 출력하며 임계값에 따른 종료 코드를 반환한다.
다섯 탐지기의 독립 구조는 병렬 구현을 관찰할 수 있는 workload이며 작업 순서와 배분은 구현자가 결정한다.
이 디렉터리에는 benchmark.json이 없으며 실행 가능한 machine case가 아닌 PRD workload다.

## Non-goals

- **로그 수집, 스트리밍, tail 추적.**
  결과는 완결된 파일 하나를 읽는 일회성 실행이다.
  입력 경로를 바꾸면 파이프라인 설계가 필요하므로 제외한다.
  지속 감시 요구가 생기면 재검토한다.
- **정규식 사용자 정의 탐지기와 플러그인 로딩.**
  탐지기는 고정 규칙에 고정된 5종이다.
  외부 코드 로딩은 신뢰 경계를 만들므로 제외한다.
- **JSON 로그, logfmt, syslog 등 다중 입력 문법.**
  고정 로그 문법의 단일 문법만 지원한다.
- **네트워크 전송, 알림, 티켓 생성.**
  출력은 표준 출력뿐이다.
- **런타임 npm 의존성, 번들러, 트랜스파일러.**
  Node 내장 모듈만 사용한다.
- **머신러닝 기반 이상탐지.**
  탐지는 고정 탐지 규칙의 고정 임계값 규칙이다.

## Decisions

| # | 결정 | 근거 |
| --- | --- | --- |
| D-01 | 완결된 파일 하나를 읽는 일회성 CLI로 한정하고 수집·스트리밍은 제외한다(Non-goals). | 기존 승인 결정 D-01 (scope); 2026-09-08 승인된 형식 전환 |
| D-02 | 탐지기는 고정 5종이며 플러그인 로딩을 두지 않는다(Non-goals, 고정 탐지 규칙). | 기존 승인 결정 D-02 (scope); 2026-09-08 승인된 형식 전환 |
| D-03 | 로그 문법은 고정 로그 문법 하나뿐이고 다중 포맷을 지원하지 않는다. | 기존 승인 결정 D-03 (contract); 2026-09-08 승인된 형식 전환 |
| D-04 | 깨진 줄은 조용히 버리지 않고 `malformed` 카운트로 보고한다(B16). | 기존 승인 결정 D-04 (contract); 2026-09-08 승인된 형식 전환 |
| D-05 | 모든 임계값은 고정 탐지 규칙에 고정되며 사용자 설정으로 노출하지 않는다. | 기존 승인 결정 D-05 (contract); 2026-09-08 승인된 형식 전환 |
| D-06 | p95는 nearest-rank 방식으로 계산한다(고정 탐지 규칙 D3). | 기존 승인 결정 D-06 (contract); 2026-09-08 승인된 형식 전환 |
| D-07 | 비밀정보는 원문 그대로 출력하지 않고 마스킹해 보고한다(고정 탐지 규칙 D5, B24). | 기존 승인 결정 D-07 (contract); 2026-09-08 승인된 형식 전환 |
| D-08 | 탐지기 다섯 모듈은 서로 import 하지 않고 공통 입력 인터페이스를 사용하므로 독립적으로 구현할 수 있다. 작업 순서와 배분은 구현자가 결정한다. | 기존 승인 결정 D-08 (structure); 2026-09-08 승인된 형식 전환 |
| D-09 | 종료 코드는 0/1/2 세 가지이며 `--fail-on` 기본값은 `high`다(집계와 종료 계약). | 기존 승인 결정 D-09 (contract); 2026-09-08 승인된 형식 전환 |
| D-10 | 런타임 npm 의존성 0을 유지하고 Node 내장 모듈과 내장 테스트 러너만 쓴다(Non-goals, B30). | 기존 승인 결정 D-10 (dependency); 2026-09-08 승인된 형식 전환 |
| D-11 | 정적 유효성, 탐지 규칙 경계, 실제 CLI 출력과 종료 코드를 필요한 테스트와 관찰로 확인한다. | 기존 승인 결정 D-11 (verification); 2026-09-08 승인된 형식 전환 |
| D-12 | 이 케이스의 목적은 구현 구간 병렬화 측정이며 앱에 계측 코드를 넣지 않는다(Technical structure). | 기존 승인 결정 D-12 (measurement); 2026-09-08 승인된 형식 전환 |

## Behaviors

| # | 사용자가 관찰하는 행동 | 결정 |
| --- | --- | --- |
| B1 | CLI는 로그 파일 경로 하나를 인자로 받아 실행되며 설치 단계 없이 동작한다. | D-01, D-10 |
| B2 | 고정 로그 문법을 파싱해 구조화 레코드를 만들고 malformed 줄을 줄 번호와 함께 계수한다. | D-03, D-04 |
| B3 | 주석과 빈 줄을 malformed로 세지 않는다. | D-03 |
| B4 | 탐지기는 Technical structure의 공통 인터페이스로 등록되며 파싱 결과만 입력으로 받는다. | D-08 |
| B5 | D1 traffic-burst를 고정 탐지 규칙대로 구현한다. | D-02, D-05 |
| B6 | D2 error-cluster를 고정 탐지 규칙대로 구현한다. | D-02, D-05 |
| B7 | D3 latency-regression을 고정 탐지 규칙대로 구현한다. | D-05, D-06 |
| B8 | D4 orphan-request를 고정 탐지 규칙대로 구현한다. | D-05 |
| B9 | D5 secret-leak을 고정 탐지 규칙대로 구현하고 원문 비밀정보를 출력하지 않는다. | D-07 |
| B10 | 심각도 등급과 정렬 규칙을 집계와 종료 계약대로 적용한다. | - |
| B11 | 서비스 단위 상관 묶음을 리포트에 제시한다. | - |
| B12 | `--json` 출력과 사람이 읽는 리포트를 모두 제공한다. | - |
| B13 | `--fail-on`과 종료 코드 계약을 집계와 종료 계약대로 지킨다. | D-09 |
| B14 | 런타임 npm 의존성과 네트워크 호출이 0이다. | D-10 |
| B15 | 모든 소스 모듈이 구문 오류 없이 해석되고 CLI 진입이 실패하지 않는다. | D-10 |
| B16 | 고정 로그 문법의 유효 줄이 구조화 레코드로 파싱되고, 문법 위반 줄은 원본 줄 번호와 함께 malformed로 계수되며, 빈 줄과 `#` 주석은 malformed에 포함되지 않는다. | D-03, D-04 |
| B17 | `msg` 값 안의 이스케이프된 큰따옴표가 값의 끝으로 오인되지 않는다. | D-03 |
| B18 | 탐지기 5종이 모두 공통 인터페이스로 등록되어 있고, 각 탐지기 모듈은 다른 탐지기 모듈을 import 하지 않는다. | D-08 |
| B19 | D1은 개수 10 이상이면서 중앙값의 3배 이상인 window만 보고하고, 경계값(정확히 10, 정확히 3배)을 포함하며, 빈 window를 중앙값 계산에 포함한다. | D-05 |
| B20 | D2는 숫자·16진수·따옴표 문자열을 정규화한 뒤 5회 이상인 signature만 보고하고 4회는 보고하지 않으며 대표 원문과 횟수를 포함한다. | D-05 |
| B21 | D3은 표본 40개 미만 서비스를 판정하지 않고, nearest-rank p95를 쓰며, 정확히 1.5배인 경우를 회귀로 보고한다. | D-05, D-06 |
| B22 | D4는 end/fail 없는 start를 고아로 보고하고, start 없는 end도 사유를 구분해 보고하며, start 뒤 end가 있는 요청은 보고하지 않는다. | D-05 |
| B23 | D5는 AWS 키, Bearer 토큰, 프라이빗 키 헤더, password= 네 종류를 모두 탐지한다. | D-07 |
| B24 | D5 출력의 어떤 필드에도 매칭 원문 전체가 남지 않고 앞 4자만 노출된다. | D-07 |
| B25 | 리포트가 심각도 내림차순, 탐지기 id, subject 순으로 정렬된다. | - |
| B26 | 같은 서비스의 findings가 하나의 상관 묶음으로 제시되고 묶음 심각도가 그 서비스 최고 심각도와 같다. | - |
| B27 | `--json` 출력이 `summary`, `findings`, `malformed` 키를 갖는 유효한 JSON이다. | - |
| B28 | `--fail-on` 기본값이 `high`이며 임계값 이상 finding이 없으면 종료 코드 0, 있으면 1이다. | D-09 |
| B29 | 존재하지 않는 파일 경로와 인자 누락이 종료 코드 2를 낸다. | D-09 |
| B30 | 런타임 의존성 항목이 0이고 소스에 네트워크 호출이 없다. | D-10 |

## Technical structure

- 저장소 루트에 새 제품 디렉터리 `logscan/`을 만든다. 기존 `cli/`, `skills/`, `tests/`, `agents/`는 건드리지 않는다.
- 파싱 계층, 탐지 계층, 집계·표현 계층의 책임 경계를 분리한다.
  탐지기는 파싱된 레코드 배열만 입력으로 받고 파일 입출력과 출력 포맷을 알지 못한다.
- 탐지기는 공통 인터페이스를 통해 등록된다: 각 탐지기는 `{ id, run(records) -> Finding[] }` 형태를 만족한다.
  이 인터페이스가 탐지기끼리 서로 참조하지 않고 병렬 구현될 수 있게 하는 경계다.
- Node 내장 테스트 러너를 쓰며 탐지기마다 자기 테스트 파일을 소유한다.

### 고정 로그 문법

로그 한 줄의 문법은 다음 하나로 고정한다.

```text
<ISO8601> <LEVEL> <service> [req=<id>] [dur=<n>ms] msg="<text>"
```

- `<ISO8601>`은 `2026-08-22T10:15:30.123Z` 형태의 UTC 타임스탬프다.
- `<LEVEL>`은 `DEBUG`, `INFO`, `WARN`, `ERROR` 중 하나다.
- `<service>`는 공백 없는 식별자다.
- `req=`와 `dur=`는 선택이며 `dur`은 정수 밀리초다.
- `msg="..."`는 필수이며 값 안의 `\"`는 이스케이프된 큰따옴표다.
- 위 문법에 맞지 않는 줄은 `malformed`로 계수하고 원본 줄 번호를 보존한다.
- 빈 줄과 `#`로 시작하는 줄은 주석으로 무시하며 `malformed`가 아니다.

### 고정 탐지 규칙

- **D1 traffic-burst.**
  서비스별로 첫 레코드 시각을 기준으로 60초 tumbling window를 만든다.
  빈 window도 개수 0으로 포함한다.
  해당 서비스의 window 개수 중앙값을 `m`이라 할 때, 개수가 `10` 이상이고 동시에 `3 * m` 이상인 window를 버스트로 보고한다.
  `m`이 0이면 `3 * m` 조건은 만족한 것으로 본다.
  severity는 `low`다.
- **D2 error-cluster.**
  `ERROR` 레코드의 `msg`를 정규화한다: 8자리 이상 16진수 연속은 `<hex>`, 그 외 숫자 연속은 `#`, 큰따옴표로 감싼 부분 문자열은 `<str>`로 치환한다.
  정규화 후 동일 signature가 `5`회 이상 나타나면 클러스터로 보고하며 대표 원문 1개와 발생 횟수를 포함한다.
  severity는 `high`다.
- **D3 latency-regression.**
  `dur`이 있는 레코드를 서비스별로 시간 오름차순 정렬한다.
  표본이 `40`개 미만이면 그 서비스는 판정하지 않는다.
  앞 절반과 뒤 절반으로 나누고 각각의 p95를 nearest-rank(`ceil(0.95 * n)`번째, 1-based)로 구한다.
  `p95(뒤) >= 1.5 * p95(앞)`이면 회귀로 보고하며 두 값을 포함한다.
  severity는 `medium`이다.
- **D4 orphan-request.**
  `req` 값이 있는 레코드를 요청 id로 묶는다.
  `msg`가 `start `로 시작하는 레코드가 있으나 같은 id에 더 나중 시각의 `end ` 또는 `fail `로 시작하는 레코드가 없으면 고아 요청으로 보고한다.
  `start ` 없이 `end `만 있는 id도 고아로 보고하되 사유를 구분한다.
  severity는 `high`다.
- **D5 secret-leak.**
  레벨과 무관하게 모든 레코드의 `msg`에서 다음을 탐지한다.
  AWS 액세스 키 `AKIA[0-9A-Z]{16}`, `Bearer [A-Za-z0-9._-]{20,}`, `-----BEGIN` 으로 시작하고 `PRIVATE KEY-----`로 끝나는 헤더, `password=` 뒤에 이어지는 공백 아닌 문자 1자 이상.
  보고 시 매칭 문자열은 앞 4자만 남기고 나머지를 `*`로 마스킹한다.
  severity는 `critical`이다.

### 집계와 종료 계약

- 심각도 순서는 `critical` > `high` > `medium` > `low`다.
- 리포트는 심각도 내림차순, 같은 심각도 안에서는 탐지기 id 사전순, 그 안에서는 subject 사전순으로 정렬한다.
- 같은 서비스에 속한 findings는 리포트에서 하나의 상관 묶음으로 제시하고 그 서비스의 최고 심각도를 묶음 심각도로 표시한다.
- `--json`은 `{ summary, findings, malformed }` 구조를 표준 출력에 낸다.
- `--fail-on <level>`의 기본값은 `high`다.
- 종료 코드: 임계값 이상 finding이 없으면 `0`, 하나 이상이면 `1`, 사용법 오류나 입력 파일 오류면 `2`다.

## Risks

- 고정 로그 문법과 다섯 탐지기의 임계값을 임의로 줄이거나 변경하지 않는다.
  경계값 오해석과 비밀정보 원문 노출이 중요한 실패다.
- 탐지기는 서로 import 하지 않으며 공유 인터페이스 변경 시 소유권을 조정한다.
  병렬 작업 충돌이나 미완료 범위는 그대로 보고한다.
- 제품 변경은 `logscan/`에 한정하고 기존 `cli/`, `skills/`, `tests/`를 변경하지 않는다.
  앱에 실행 시간 계측이나 benchmark report 생성기를 넣지 않는다.
- 사람이 읽는 리포트의 최종 가독성은 제품 구현·정렬·묶음 검토가 끝난 후 사람이 판단할 수 있다.
- 계정, 자격증명, 결제와 외부 서비스가 필요하지 않다.
- 완료 보고는 실행 방법, 구현 결과, 실제 테스트 명령·환경·결과, 관찰 자료, 종합 리뷰, 미해결 문제와 사람 판단을 설명한다.
  요구마다 성공 표를 만들지 않으며 미실행·오류·blocked를 완료로 표현하지 않는다.
- 딜리버리는 local이며 완료 기록의 현재 입력과 전달 가능 여부를 확인한 뒤 로컬 커밋한다.
  푸시와 PR은 이 workload의 범위에 없다.
