ROLE: Implementor and in-session benchmark executor. Confirm SASU_HERDR_ROLE using the pane environment. Do not dispatch another implementation agent and do not impersonate another session.
PIPELINE: Read /tmp/sasu-full-implementation-comparison/old/.agents/skills/benchmark-implement/SKILL.md and the pinned sibling implement skill and its applicable references. Use that version's workflow in this fresh session.
ORIGINAL INVOCATION: 음 이제 구, 신 버전의 같은 요구사항의 PRD는 있는거야? 그리고 그걸 예전 버전과 최신버전으로 돌려보면 되지 않아? 그러면 구현속도와 어디서 시간썻는지 그런 병목지점 비교가 다 되는거아닌가? 어려워서 그런거야??
GOAL AND CONTEXT: Execute the approved six-behavior task-list library PRD from absent product source through actual tests, version-required verification, repairs when needed, and a current final receipt. This session is one isolated implementation benchmark. Actual executor is Codex gpt-5.6-sol medium. Do not consult any previous implementation, other run, pilot report, memory of solutions, or the other version. The PRD is the complete product contract. Measurement is outside the product contract.
AUTHORITY: Reversible implementation decisions are autonomous within the PRD. Preserve supplied tests, PRD, configuration, and benchmark inputs. Add useful tests if needed, without weakening supplied expectations. Do not change harness source, global installs/config, another project, or judge profiles. No fake evidence, skipped gates, diagnostic judge override, grant, reset, new run after a failure, or unauthorized adoption. Follow the version's real repair bounds. On a genuine unresolved prerequisite or exhausted/unrecoverable run, emit OBSERVER_BLOCK with exact state and reason. Do not use interactive question tools.
SOURCE: /tmp/sasu-full-implementation-comparison/old. Fixed case benchmarks/task-list-full/benchmark.json and approved PRD benchmarks/task-list-full/prd.md.
DIRTY ATTRIBUTION: pre-existing. Prepared benchmark case/PRD copies are input material, not your product changes.
VERSION BINDING: Every Sasu CLI invocation must use `node /Users/hoyeonlee/projects/sasu.worktrees/workflow-baseline/cli/dist/cli.js`. This is contract 0.8.0. Do not call the global sasu, global implement skill, or a different checkout. The staged skill's auxiliary scripts are linked to this exact source, preserving their native harness-root lookup. Reads of pinned CLI help/source for a concrete command question are permitted; no prior product solution reads.
FIRST RUN ACTION: After reading the required skill and complete PRD, from the SOURCE root run `node /Users/hoyeonlee/projects/sasu.worktrees/workflow-baseline/skills/benchmark-implement/scripts/benchmark_report.js prepare-run --case benchmarks/task-list-full/benchmark.json`. Do this exactly once. Use only the returned fresh worktree/PRD/run ID for implementation. If prepare fails, report it; do not fall back to the launcher or hand-create run state.
EXECUTION: Run the approved PRD through the pinned implement skill in THIS session, start before creating product source, implement all six behaviors, observe the actual public module, run required version-specific evidence/check/verify/finalize steps, and repair findings within normal bounds. Work through completion autonomously. Do not invent a new completion prerequisite about benchmark reports, installation, or receipt consumers. Keep actual execution and provenance honest.
RETURN CONTRACT: Stop the measured implementation window at the CLI receipt. Then report exact prepared worktree, run ID, state and receipt paths, native session ID, final status, actual checks/verdicts, remaining findings and autonomous choices. Do not start the post-receipt evaluator or reporter in this turn: the coordinator will schedule those after both measured implementations so analysis does not compete with the second run. Preserve result records. Local ordinary preservation commit of your own product work is authorized after receipt; no ship, push, PR, deployment or global install is requested.
