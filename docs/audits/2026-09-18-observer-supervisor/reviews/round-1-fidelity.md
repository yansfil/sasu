### Fix now

1. **D-15 "status shows which loop owns recovery" is not implemented — `recoveryOwner` is write-only.**
   `dispatch` parses and records `recoveryOwner` (`cli/src/supervisor/policy.ts:27`, written via `cli/src/implement/commands.ts:566,594,620`, persisted in `state.json` by `cli/src/implement/store.ts`), but nothing in `cli/src/supervisor/*.ts` ever reads the field back — `decide.ts`, `tick.ts`, `commands.ts:status`/`supervisorStatusView`, and doctor never reference `recoveryOwner`. Grep confirms zero matches for `recoveryOwner` under `cli/src/supervisor`. D-15's own comment says "status shows which loop owns recovery" and B18 implies the two control loops are distinguishable at read time; today that distinction exists only in dispatch's one-line human message, not in `sasu supervisor status --json` or `sasu doctor`. Functionally this is low risk — D-06 already makes the supervisor structurally incapable of auto-replacing an Observer regardless of `recoveryOwner`'s value, so no double-input bug follows — but the decision as written is not delivered, and there is no test asserting `recoveryOwner` is visible anywhere after dispatch. Either wire it into `supervisorStatusView`/doctor output, or narrow D-15's wording to match what actually ships (recorded-but-not-yet-surfaced).

### Follow-up improvements

- The mutation-testing evidence (`agents/runs/sasu-observer-supervisor/artifacts/qa/mutation.log`) reports only 5 planted bugs against `decide.ts`/`tick.ts` with a stated non-full-mutation-score caveat. Given this module is the whole safety argument for B4/B6/B9 (zero cross-session input), a slightly larger mutation pass (e.g. also mutating `judgeObserver`'s two identity comparisons and the `answered()` episode-token filter) would be cheap and would materially firm up the "senior sitting next to the implementation" bar for this specific file.
- `renderWake` / wake body construction (`cli/src/supervisor/wake.ts`, D-07/B10) wasn't inspected line-by-line in this pass beyond its call site in `tick.ts:182`; worth a spot-check that it never interpolates PRD/task content (only run instance, Observer UUID, reason) since that's the load-bearing claim for "0 actions on a wrong-session delivery."
- `LOG_CAP_BYTES` rotation in `tick.ts:61-69` keeps at most two files (`tick.log`, `tick.log.1`) — fine per engineering 15, but there's no cap on `SupervisorIndex.entries` count itself (only on `removed` history via `REMOVED_HISTORY_CAP`). A pathological number of concurrently dispatched runs would make each tick's index rewrite grow unbounded; not a near-term concern given real usage patterns, but worth a note if run volume grows.

### What was checked

- PRD (`agents/runs/sasu-observer-supervisor/prd.md`): all 16 Decisions and all 20 Behaviors read against source.
- Deterministic verification report (PASS) and its registered evidence descriptions (`docs/audits/2026-09-18-observer-supervisor/`, referenced but not independently re-run beyond what's already GREEN in the report — I did not re-execute the suites since I'm read-only and the report already carries the full-suite GREEN).
- Source: `cli/src/supervisor/decide.ts` (full), `tick.ts` (full), `index.ts` (full), `stop-hook.ts` (full), `launchd.ts` (partial, plist/status/install logic), `digest.ts` (partial), `policy.ts` (full), `commands.ts` (dispatch/digest/status/install/uninstall/handover sections), `cli/src/implement/herdr.ts` (`getAgent`/`promptAgent`/guarded-prompt argv sections).
- `skills/implement/references/observer-and-herdr.md` for D-13/B19 (confirmed await/re-arm language removed from current docs; remaining "re-arm" hits are frozen historical audit logs under `docs/audits/`, not current instructions).
- Test coverage cross-referenced by behavior tag: `cli/test/unit/supervisor-decide.test.mjs`, `supervisor-tick.test.mjs`, `supervisor-herdr.test.mjs`, `supervisor-index.test.mjs`, `supervisor-launchd.test.mjs`, `cli/test/e2e/supervisor.test.mjs`, `cli/test/e2e/implement-dispatch.test.mjs`, `tests/install_local_skills.test.mjs`. Confirmed explicit B-tag coverage for B1-B18, B20; B19 has no direct test (expected — it asserts an absence/doc claim) and B2 is covered by `implement-dispatch.test.mjs:139,179`.
- Verified `recoveryOwner` (D-15) is written at dispatch but never read anywhere under `cli/src/supervisor/`, and no test asserts it surfaces in status/doctor output — this is the one substantive gap found (see Fix now).
- The five declared deviations (a-e in the task brief) all check out against source: (a) index `missingTicks`/level-triggered recompute replaces process-memory backoff (`index.ts:33`, `tick.ts:79-89`); (b) terminal_id mismatch on herdr restart → `observer-gone` with instruction to run `sasu supervisor handover` (`decide.ts:74-76`); (c) `status --digest` refuses non-Observer sessions (`commands.ts:1116`); (d) `tick.ts` never touches git — only `digest.ts` does, confirmed by absence of any `git`/`spawnSync` call in `tick.ts`; (e) real launchd bootstrap unrun, confirmed by `real-domain-untouched.txt` evidence description and absence of a live-domain test (only isolated-HOME `supervisor-launchd.test.mjs` and e2e `B14` test exercise it). All five stay inside the approved contract; none need further human input beyond what the PRD already flags as risk (Risks section, LaunchAgent install point).