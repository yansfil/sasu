Your read of the mechanism is correct. I confirmed every link in source: `runner.ts:204` folds `mutatedTree` into `green`, `commands.ts:2155` persists `outcome` from `green` alongside the true exit code and drops `mutatedTree`, `store.ts:299` recomputes green from exit code, timeout and signal only, and throws on disagreement. `persistState` at `store.ts:802` never runs the parser, so nothing stands between the writer and the disk. Two things your framing does not have belong ahead of Q1 to Q4.

**The suite axis has the same defect in the opposite disguise.** At `commands.ts:2116` the mechanical record is written as `exitCode: result.green ? 0 : (result.exitCode === 0 ? 1 : result.exitCode)`. When a command exits 0 but moves the tree, the AC ledger keeps the true exit code and violates its invariant, while the suite ledger falsifies the exit code to preserve its invariant. Same dropped bit, two different lies, one of them silent. That is a class, not a field, and it is direct evidence for PRINCIPLES 3 and 10.

**These are two independent defects, and neither fix alone un-bricks herdr-ide.** Fix the schema and verify still fails every Rust or SwiftPM run on purpose: the batch-level `treeMoved` at `runner.ts:217` fires on `target/` just as the per-unit flag does, and the recorded `tree` fingerprint is captured after the build at `runner.ts:158`, so the attempt names a tree its exit code was not earned on. Fix the exclusions and the existing state file still refuses to load. Both must land, and there is a third piece for the file already on disk.

**Q3 matters most, and the fix is small.** `outcome` is a value derived from three other fields on the same record, and three places derive it independently: `checks.ts:360`, `commands.ts:2155`, and the reader at `store.ts:299`. The reader itself documents that this species has happened before: the park comment at `store.ts:310` says "the write path and the read path disagreeing about one field, the same species as the verb vocabulary." This is the third occurrence. Two changes close the class rather than the instance. First, one `attemptOutcome(inputs)` function that the two writers and the validator all call, so there is exactly one definition. Second, `persistState` parses the serialized text with `parseImplementState` before writing it. That turns every future writer/reader drift into a write-time refusal with nothing on disk, which is what "records stay honest and singular" actually requires. The unit test at `implement-runner.test.mjs:140` already asserts the rejection in memory; extending it one step, persist then reload, is the regression. Making the illegal state unrepresentable in the type system would not have caught this, because the contradiction lives between two boolean fields both of which are legal on their own.

**Q1: honor git, because the harness already defines judged source that way elsewhere.** `cli/lib/git.js:551` builds the judged diff from `git diff` plus `git ls-files --others --exclude-standard`. So the gate side already says judged source is git-visible content, and `store.ts:857` says it is everything under the root minus six hardcoded names. Two definitions of one concept in one harness violates PRINCIPLES 3 and 12 before the incident is even considered. The right shape is the one `dirtySourcePaths` at `store.ts:906` already uses: `git ls-files --cached --others --exclude-standard -z` when `isGitWorkTree`, the readdir walk as the non-git fallback. Your listed costs: non-git projects keep today's behavior, so no regression. Gitignored-but-judged source is already invisible to the judge that reads the diff, so consistency here costs nothing new. One `ls-files` call per snapshot is cheaper than the walk it replaces. Tracked-but-ignored files are listed by `--cached`, so they stay judged. The performance claim is real and independent of correctness. `target/` in herdr-ide is currently 8.8 GB across about 58,800 files, and `captureSourceSnapshot` reads and hashes every one of them, twice per unit plus twice per batch, so a 6-unit verify hashed roughly 120 GB. That is also a correctness problem under PRINCIPLES 11: incremental builds make `mutatedTree` nondeterministic on artifact directories, which is the coin-flip guard the principle warns about. Keep a small hardcoded set only as the non-git fallback, never as the primary rule. One correction: the `.gitignore` that lists `target` and `.build` is herdr-ide's, not sasu's.

**Q2: your proposed shape recreates the disease one level up.** The validator at `store.ts:384` derives `check.status` from the latest attempt's `outcome`. If `outcome` becomes process-only and `attributeToCriteria` keeps setting `status` from `green` (which still folds in `mutatedTree`), you get `outcome:"green"` beside `status:"pending"`, and the loader throws on the next line down. The shape that does not leak: `outcome` stays the single verdict and gains a third value, `"tree-moved"`, `mutatedTree` is persisted as an input beside `exitCode`, and the validator derives all three values from four inputs through the same shared function. `failureClass` should be null for `tree-moved`, because `updateFailureDecisions` at `checks.ts:287` counts same-class failures to open decision points, and a tree violation is not an output class. The reason then survives in state rather than in stderr, which PRINCIPLES 10 already requires. An append-only schema makes this cheap: a new field with a loader default and a new enum value are additive, and the validator is the migration point. The `checks.ts:345` policy of ignoring `mutatedTree` on single checks stays coherent under this shape, because the flag is recorded either way and the verdict policy is applied once, in one function, with the batch/single difference as an explicit argument rather than two ad-hoc expressions.

**Q4: a targeted loader migration, not a repair verb.** A generic `repair` command would give agents a way to rewrite the ledger and is exactly the escape hatch PRINCIPLES 4 asks to be accounted for. But the harness already repairs its own poison: `commands.ts:3126` drops artifacts on harness-owned paths during verify. The honest equivalent here is a migration in `parseImplementState` keyed on the exact shape only the old writer could produce: exit 0, no timeout, no signal, `outcome:"failed"`, and `mutatedTree` absent. That shape has one possible origin, the writer at `commands.ts:2155` on a moved tree, so stamping `mutatedTree:true` and `outcome:"tree-moved"` records what the harness knew and dropped, not what anyone claims. No new verb, no flag, and `retire` and `status` start working on herdr-ide without a special case. The failed verify attempt stays in the ledger as a real failure of the old exclusion rule, which is the truthful record.

Priority order for landing: shared outcome function plus write-time parse (Q3), git-backed file listing (Q1), the `tree-moved` value with `mutatedTree` persisted (Q2), the loader migration (Q4). The first is the one that makes the fourth safe to write.
