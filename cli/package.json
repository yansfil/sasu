{
  "name": "checkshirt-cli",
  "version": "0.3.1",
  "private": true,
  "description": "checkshirt harness CLI: judge gates, verification runner, and absorbed implement state library",
  "type": "commonjs",
  "bin": {
    "checkshirt": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test \"test/unit/*.test.mjs\"",
    "test:e2e": "node --test \"test/e2e/*.test.mjs\"",
    "test:smoke": "node --test \"test/smoke/*.test.mjs\""
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
