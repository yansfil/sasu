{
  "name": "sasu-cli",
  "version": "0.11.0",
  "private": true,
  "description": "sasu CLI: document gates and deterministic implementation verification",
  "type": "commonjs",
  "bin": {
    "sasu": "dist/cli.js",
    "hcoord": "dist/hcoord/cli.js"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test \"test/unit/*.test.mjs\"",
    "test:e2e": "node --test \"test/e2e/*.test.mjs\"",
    "test:smoke": "node --test \"test/smoke/*.test.mjs\""
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
