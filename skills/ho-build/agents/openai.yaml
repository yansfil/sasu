interface:
  display_name: "ho-build"
  short_description: "Implement PRDs with goals, graph, and proof"
  default_prompt: "Use $ho-build to implement the approved PRD with goal tracking, TaskGraph, fidelity checks, final review, and artifact-backed proof."
