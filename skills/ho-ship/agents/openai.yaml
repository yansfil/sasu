interface:
  display_name: "ho-ship"
  short_description: "Ship PRD work through PR, CI, and approved merge"
  default_prompt: "Use $ho-ship to publish a completed PRD implementation, watch CI, and merge only with explicit approval."
