interface:
  display_name: "implement"
  short_description: "Implement PRDs with tasks, evidence, and receipts"
  default_prompt: "Use $implement to execute the approved PRD with a validated task plan, artifact-backed proof, profile-aware review, and a strict receipt."
