interface:
  display_name: "implement"
  short_description: "Implement PRDs with deterministic checks and visible review"
  default_prompt: "Use $implement to implement the approved PRD, collect actual evidence, run deterministic verification, ask native review subagents for advice, fix current-scope defects, and deliver the current result."
