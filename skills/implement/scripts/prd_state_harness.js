#!/usr/bin/env node
"use strict";

process.stderr.write("prd_state_harness.js was removed. Use `sasu implement start|check|park|resume|task|artifact|status|verify|finalize`; use `sasu prd readiness --prd <path>` for PRD readiness.\n");
process.exit(2);
