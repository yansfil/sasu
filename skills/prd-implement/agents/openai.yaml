interface:
  display_name: "Fulfill (PRD Implement)"
  short_description: "Implement PRDs with goals, graph, and proof"
  default_prompt: "Use $fulfill to implement the approved PRD with goal tracking, TaskGraph, main-agent fidelity checks, final adversarial review, and artifact-backed proof."
