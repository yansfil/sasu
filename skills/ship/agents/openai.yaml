interface:
  display_name: "ship"
  short_description: "Deliver PRD work locally or through PR, CI, and approved merge"
  default_prompt: "Use the post-receipt delivery workflow: validate a current delivery-eligible receipt and commit local-mode work locally, or use $ship for PR delivery and merge only with explicit approval."
