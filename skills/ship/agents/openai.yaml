interface:
  display_name: "ship"
  short_description: "Deliver PRD work locally or through PR, CI, and approved merge"
  default_prompt: "Validate the current deterministic verification report, then commit local-mode work or deliver it through a pull request, CI, and an explicitly approved merge."
